{
  "name": "sport-scale-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run -w apps/sport_scale_app dev",
    "build": "npm run -w apps/sport_scale_app build",
    "test": "npm run -w apps/sport_scale_app test",
    "smoke": "npm run build"
  }
}
